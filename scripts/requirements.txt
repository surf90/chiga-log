# 実行系スクリプト（GitHub Actionsから呼ばれるもの）は標準ライブラリのみで動作するため、
# 本番では pip install は不要です。
#
# 以下は開発時のみ使用するツールです:
#   pip install -r scripts/requirements.txt
#   pytest tests

pytest>=8.0

# Windows は zoneinfo のタイムゾーンDBを同梱しないため、ZoneInfo("Asia/Tokyo") を
# 使うテスト（tests/test_fetch_heatstroke_alert.py 等）に必要。
# 本番の GitHub Actions（Linux）は OS 側の DB を使うので条件付きで入れる。
tzdata>=2024.1; sys_platform == "win32"
